<div class="govuk-grid-row">
  <div class="govuk-grid-column-two-thirds">
    <%= render("waste_carriers_engine/shared/back", back_path: new_resource_payment_form_path(@resource._id)) %>

    <%= render("waste_carriers_engine/shared/errors", object: @missed_card_payment_form) %>

    <h1 class="govuk-heading-l">
      <%= t(".heading", reg_identifier: @resource.reg_identifier) %>
    </h1>

    <%= render(
      "shared/payment_form",
      form: @missed_card_payment_form,
      path: resource_missed_card_payment_forms_path(@resource._id),
      resource: @resource
    ) %>
  </div>
</div>
