<div class="grid-row">
  <div class="column-two-thirds">
    <%= render("waste_carriers_engine/shared/back", back_path: new_resource_payment_path(@resource._id)) %>

    <h1 class="heading-large">
      <%= t(".heading", reg_identifier: @resource.reg_identifier) %>
    </h1>

    <%= render(
      "shared/payment_form",
      form: @postal_order_payment_form,
      path: resource_postal_order_payment_forms_path(@resource._id),
      resource: @resource
    ) %>
  </div>
</div>
